node_modules/
dist/
.env
.env.local
.DS_Store
*.log
.saa-dump-audit.txt

# Private calibration sources. Never publish raw exam-dump PDFs.
question-imports/**/raw/*.pdf

# Claude Code local settings (per-developer)
.claude/settings.local.json

# Scratch/working dirs from local tooling — not part of the app
.docdeps/
.task-docdeps/
.tmp-vc-health/
.tmp-vital-check/
.tmp_doc_render/
tmp/

# Local data snapshots captured by the dev server — contain credentials
local-backups/
